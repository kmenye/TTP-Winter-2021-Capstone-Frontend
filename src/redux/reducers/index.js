{/*import axios from "axios";
import { bindActionCreators } from "redux";
import {GOT_ALL_TEXTILE_ZIP} from "redux";

const initialState = {

    zipDataT = [],

}

const gotTextiles = (payload) => ({
    type: GOT_TEXTILES,
    payload,
});

export const getTextiles = () => {
    console.log("ABOUT TO THUNK");
    return async (dispatch) => {
        try {
            
            const {data} = await axios.get('http://')
        }
    }
}*/}